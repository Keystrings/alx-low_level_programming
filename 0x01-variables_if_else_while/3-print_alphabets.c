#include <stdio.h>

/**
 *main- program point of entry
 *Return:0 if no error, non zero if error
 */
int main(void)
{
	char i;
	chari j;

	for (i = 'a'; i <= 'z'; i++)
	{iiii
		putchar(i);
	}
	for (j = 'A'; j <= 'Z'; j++)
	{
		putchar(j);
	}
	putchar('\n');
	return (0);
}
